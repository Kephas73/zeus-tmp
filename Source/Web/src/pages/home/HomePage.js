import { Button } from '@material-ui/core';
import React from 'react';

const HomePage = () => {
  return <div>
    <h1>Welcome to the Home Page!</h1>
    <Button variant="contained" href="host">HostPage</Button>
  </div>;
};

export default HomePage;
